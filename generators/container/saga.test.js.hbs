/**
 * Test {{ camelCase name }} sagas
 */

/* eslint-disable redux-saga/yield-effects */
import { takeLatest } from 'redux-saga/effects'
import {{ properCase name}}Saga, { defualtFunction } from '../saga'
import { {{ properCase name}}Types } from '../reducer'

describe('{{ camelCase name }}Saga Saga', () => {
  const generator = {{ camelCase name }}Saga()

  it('should start task to watch for DEFAULT_ACTION action', () => {
    expect(generator.next().value).toEqual(
      takeLatest({{ camelCase name }}Types.DEFAULT_ACTION, defualtFunction)
    )
  })
})