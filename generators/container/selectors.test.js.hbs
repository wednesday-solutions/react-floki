import { fromJS } from 'immutable'
import { select{{ properCase name }}Domain } from '../selectors'

describe('select{{ properCase name }}', () => {
  let mockedState

  beforeEach(() => {
    mockedState = {
      {{ camelCase name }}: fromJS({})
    }
  })

  it('should select the user state', () => {
    expect(select{{ properCase name }}Domain()).toEqual(mockedState.{{ camelCase name }}.toJS())
  })
})